version: '3.9'

services:
  credit-risk-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: credit-risk-api
    ports:
      - "8000:8000"
    restart: always
    environment:
      - MODEL_URI=models:/CreditRiskLogisticModel/Production
    volumes:
      - .:/app
